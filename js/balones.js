/*class Balon {
    constructor() {
      this.x = x;
      this.y = y;
      this.size = size;
      this.image = image;
      this.personaje = personaje;
    }
  
    display() {
      image(this.image, this.x, this.y, this.size, this.size);
    }
  }
  
  */